// O template String vei com o ECMAScript 6
// ele serve para substituir a concatenação se strings
// usa-se `${}` para tamplate string

const name = "Matt";
const atributo = "estudioso";

console.log(`${name} é um aluno muito ${atributo}`);